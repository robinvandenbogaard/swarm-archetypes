swarm:
  datasources:
    data-sources:
      myDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://db/postgres
        user-name: postgres
        password: postgres
