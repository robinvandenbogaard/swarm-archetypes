swarm:
  datasources:
    data-sources:
      myDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://localhost/postgres
        user-name: postgres
        password: postgres
